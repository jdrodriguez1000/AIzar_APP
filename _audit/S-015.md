# Informe de auditoria — S-015

| Campo | Valor |
|---|---|
| Sesion | S-015 |
| Fecha | 2026-08-31 |
| Etapa | 000_preproject |
| Rama | main |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-015.md`) |

## 0. Respuesta a la auditoria anterior

Responde a `R-007` (`../AIzar_Auditor/_review/R-007.md`), que audita `_audit/S-007.md` sobre el
commit `268d3f1`. Veredicto entregado: **Con hallazgos (4)** — `F-019` (`Media`), `F-020`, `F-021`,
`F-022` (`Baja`).

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-019 — el riesgo de deriva de `_global/` (una receta futura mete ahi material excluible) vivia solo en `_audit/S-007.md` | Implementado | `T-049`, en este commit: `A-006` registra el supuesto, con como se refuta y su disparador |
| F-020 — `DT-003` esta `Implementada` pero su cuerpo seguia hablando en presente de un defecto ya corregido | Implementado | `T-050`, en este commit: el bloque de diagnostico queda fechado y en pasado. El titulo se queda igual, con su razon escrita bajo el (`D-053`) — no es discrepancia: el criterio de cierre del hallazgo apunta al cuerpo, no al titulo |
| F-021 — cita del repositorio del auditor por numero de linea, sin ancla a commit, en `S-007` | Implementado | `T-051`, en este commit: `protocol-close` y `protocol-start` exigen ahora anclar toda cita de su repositorio a `git -C <repo> log -1 --format=%h`, o declararla lectura de arbol de trabajo |
| F-022 — las dos listas del informe de auditoria (archivos tocados, tareas abiertas) se escribieron de memoria y salieron incompletas | Implementado | `T-052`, en este commit: la plantilla del informe en `protocol-close` exige generarlas con `git show --stat --name-only` y el filtro del indice de `tasks.md`, en vez de recordarlas |

Ademas de los cuatro hallazgos, la recomendacion de la seccion 5.1 de `R-007` (contrastar las
carpetas del arbol contra `PROJECT.md`) se acepta e implementa en `T-053`, y la seccion 5.2 queda
cubierta por `T-049`/`A-006` sin trabajo adicional.

## 1. Que se hizo

Archivos tocados en este commit (generado con `git show --stat --name-only --format= <commit>`
sobre el commit que contiene este informe; en el momento de escribir este borrador, el equivalente
es `git diff --name-only` mas los dos archivos que el propio cierre anade):

- `.claude/skills/protocol-close/SKILL.md` — cinco cambios: (1) el `awk` del Paso 2b se explica y
  se documenta por que filtra bloques cercados (`T-055`, leccion `L-009`); (2) nuevo **Paso 2c**,
  contraste de carpetas del arbol contra `PROJECT.md` (`T-053`, recomendacion §5.1 de `R-007`); (3)
  nuevo control en el Paso 6 — riesgos que un informe reconoce y no tienen codigo (`T-054`, leccion
  `L-011`); (4) nuevo control — toda cita del repositorio del auditor se ancla a su commit (`T-051`,
  `F-021`); (5) la plantilla del informe exige **generar** las listas de archivos y tareas abiertas
  con comandos, no escribirlas de memoria (`T-052`, `F-022`).
- `.claude/skills/protocol-start/SKILL.md` — mismo control de anclaje al commit del repositorio del
  auditor, aplicado tambien al arranque de sesion (`T-051`).
- `PROJECT.md` — fila nueva para `.claude/` en «Carpetas propias», con su nota de agnosticismo
  (`D-021`); notas sobre el control del Paso 2c y las dos filas declaradas sin carpeta a proposito
  (`T-056`, `D-054`).
- `_audit/index.md` — fila `S-007` pasa de `Pendiente` a `Con hallazgos`, con la ruta de `R-007.md`
  y los cuatro codigos; su columna `Respondida en` se rellena con `S-015`. Fila nueva `S-015.md` en
  `Pendiente`.
- `_persistence/assumptions.md` — nueva **A-006**: ningun archivo de `_global/` es material en
  transito (`T-049`, `F-019`).
- `_persistence/debt_tec.md` — el cuerpo de **DT-003** se reescribe en pasado y con fecha explicita;
  nota bajo el titulo explicando por que el titulo no cambia (`T-050`, `F-020`, `D-053`). No hay
  deuda nueva.
- `_persistence/decisions.md` — nuevas **D-051** a **D-054**: aceptacion de los cuatro hallazgos y
  la recomendacion §5.1 (`D-051`); el control de riesgos-sin-codigo en el protocolo (`D-052`); el
  titulo de una deuda no cambia al pagarla (`D-053`); el control de carpetas no lleva lista de
  excepciones dentro (`D-054`).
- `_persistence/lessons.md` — nueva **L-011**: arreglar tres veces el mismo patron como caso
  particular es no haberlo arreglado.
- `_persistence/tasks.md` — nuevas **T-049** a **T-056**, las ocho `Implementada`.
- `_persistence/progress.md` — seccion 1 (Estado general), 2 (Ultimo realizado) y 3 (Siguiente
  paso) reescritas; nueva entrada de bitacora **S-015** con su fila de indice.
- `_audit/S-015.md` — este informe (lo anade el propio cierre; no es contenido de la sesion, es su
  registro para la auditoria).

Sin construccion de producto: no hubo cambios de codigo de aplicacion. Todo el diff de esta sesion
es documental — dos skills, `PROJECT.md`, `_audit/index.md` y seis archivos de `_persistence/`.

## 2. Que NO se hizo, y por que

Tareas que siguen `No implementada` (generado filtrando el indice de `_persistence/tasks.md` por
`No implementada`; lista completa, no parcial):

- **T-004** (`Alta`, `Bloqueante`) — recibir del usuario el alcance y objetivo del proyecto. Sigue
  siendo el bloqueo de fondo de toda la etapa `000_preproject`.
- **T-016** (`Alta`, `No bloqueante`) — poblar el inventario de acciones irreversibles. Bloqueada
  por `T-004`: no puede poblarse sin alcance.
- **T-018** (`Media`, `No bloqueante`) — resolver la circularidad del criterio 6 del Gate 1.
- **T-019** (`Baja`, `No bloqueante`) — enunciar una sola vez el principio de «declarar antes».
- **T-025** (`Media`, `No bloqueante`) — juzgar las cuatro candidatas aplazadas del recetario.
- **T-026** (`Alta`, `Bloqueante`) — disenar la fase `Descubrimiento` antes de entrar en ella;
  depende de que `T-004` fije el alcance.
- **T-039** (`Media`, `No bloqueante`) — acordar con el auditor la forma de entrega del dictamen de
  Gate.
- **T-043** (`Baja`, `No bloqueante`) — poner la advertencia de no copiar `phases/` dentro de
  `_methodology/`.

Ninguna de las ocho es nueva de esta sesion: las ocho tareas que si nacieron hoy (`T-049` a
`T-056`) quedaron `Implementada` en el mismo commit.

## 3. Decisiones tomadas

- **D-051** — Los cuatro hallazgos de `R-007` se aceptan, y dos de ellos (`F-022` y §5.1) se
  mecanizan en vez de reglarse: en lugar de pedir mas cuidado, se escriben los comandos que generan
  las listas y comparan las carpetas. Alternativa descartada: dejarlo como «regla escrita sin
  comando», que es justamente la version que ya fallo. Registra ademas, sin abrir decision propia,
  que el ciclo de auditoria va con siete informes de retraso.
- **D-052** — Un riesgo que el informe reconoce necesita codigo antes de cerrar la sesion: nuevo
  control en el Paso 6 de `protocol-close`. Alternativa descartada: anotarlo como leccion y confiar
  en recordarlo — es lo que ya se hizo las tres veces anteriores y fallo las tres.
- **D-053** — El titulo de una deuda nombra el defecto y no cambia al pagarla; el cuerpo si se
  corrige y se fecha. Alternativa implicita descartada: renombrar el titulo al pagarla, lo que
  rompe el ancla que ya citan otros documentos y hace ilegible el indice.
- **D-054** — El control de carpetas del Paso 2c no lleva lista de excepciones dentro de si mismo;
  cada diferencia superviviente exige su razon escrita en `PROJECT.md` o en una `D-XXX`.
  Alternativa descartada: filtrar `_product/` y `temporal/` dentro del propio comando para que
  salga siempre limpio — se descarta porque ese filtro habria que tocarlo el dia que `_product/`
  exista, y un control que hay que editar cuando el proyecto avanza normalmente no se edita: se
  ignora.

## 4. Supuestos vigentes y riesgos

- **A-006** (nueva, `Abierto`) — ningun archivo de `_global/` es material en transito. Se apoya en
  que hoy los tres archivos de esa carpeta (`guide.md`, `changelog.md`, `sources/GUIDE.md`) son
  registro del proyecto, nunca borradores. Si resulta falso —una receta futura mete ahi una copia de
  trabajo—, hoy nada lo detectaria salvo releer a mano; el disparador declarado es el propio Paso 2c
  del cierre (`T-053`), que ya se corre por otro motivo.
- **A-004** y **A-005** (de sesiones anteriores) siguen `Abierto`, sin cambios en esta sesion.

## 5. Siguiente tarea propuesta

**T-004** (`Alta`, `Bloqueante`) — recibir del usuario el alcance y objetivo del proyecto. Es el
bloqueo de fondo de toda la etapa `000_preproject`: sin el no puede avanzar `T-025`, ni disenarse la
fase `Descubrimiento` (`T-026`), ni poblarse el inventario de irreversibles (`T-016`).

## 6. Que pedimos auditar

- **El matiz de `D-053` sobre `F-020`.** Se acepto el hallazgo pero se dejo el titulo de `DT-003`
  sin cambiar, con el argumento de que el criterio de cierre del auditor apunta al cuerpo, no al
  titulo. Pedimos que se revise si esa lectura del criterio es correcta o si el titulo tambien
  deberia haber cambiado.
- **Si el nuevo control del Paso 6 (`D-052`, riesgo-sin-codigo) es suficiente.** Busca frases que el
  propio informe reconozca como riesgo, pero depende de que el borrador las redacte con una forma
  reconocible («si algun dia X, hoy nada lo detectaria»). Un riesgo real escrito de otra forma
  seguiria sin disparar el control.
- **La cobertura real del Paso 2c (`T-053`).** Solo compara directorios de primer nivel. Un defecto
  de la misma familia dos niveles mas abajo (una subcarpeta sin declarar dentro de una carpeta si
  declarada) no lo veria.
- **El retraso del ciclo de auditoria, anotado en `D-051` pero sin codigo propio** (ni `A-XXX`, ni
  `T-XXX`, ni `DT-XXX`): `R-007` llega sobre un commit con siete informes nuestros por detras. No
  sabemos si conviene que quede como esta —depende de la cadencia del auditor, no de nosotros— o si
  deberia tener algun registro de nuestro lado.
