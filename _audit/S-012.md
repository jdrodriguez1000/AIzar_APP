# Informe de auditoria — S-012

| Campo | Valor |
|---|---|
| Sesion | S-012 |
| Fecha | 2026-08-30 |
| Etapa | 000_preproject |
| Rama | main |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-012.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-006 — `_audit/index.md:46` remite a `PROJECT.md` por la ruta del `findings.md` del auditor, y `PROJECT.md` no la tenia | Implementado | `T-032`, `Implementada` (2026-08-30). `PROJECT.md`, tabla **Rutas**, gana la fila «Estado de los hallazgos | `../AIzar_Auditor/_persistence/findings.md`». Verificado ejecutando `cat ../AIzar_Auditor/_persistence/findings.md` — existe y es legible. En este commit |
| F-007 — el bloque `bash` del Paso 1c de `protocol-start` dejo de ser una orden ejecutable (`cat "<ruta del campo «Canal de vuelta»...>"`) y las rutas de `PROJECT.md` con `\` no resuelven en Bash | Implementado | `T-033`, `Implementada` (2026-08-30). El bloque vuelve a ser `cat ../AIzar_Auditor/_review/index.md`, literal. `PROJECT.md` declara la **forma canonica POSIX** (relativa, con `/`) de sus rutas relativas, con la excepcion declarada de las dos absolutas. Decidido en `D-039`, que documenta ademas por que se conservo la indireccion (opcion (b) del hallazgo) en vez de escribir la ruta dentro (opcion (a), que se probo primero y se revirtio — `L-008`). En este commit |
| F-008 — codigos vivos del proyecto (`T-014`, `D-018`, `T-015`) sobrevivieron dentro de `protocol-close/SKILL.md` tras el barrido de `D-021` | Implementado | `T-034`, `Implementada` (2026-08-30). Las cuatro lineas con codigos vivos (`:349` y `:373-375`) pasan a `T-NNN`/`D-NNN` o a «el codigo y la ruta tal como aparecen en tu registro». **Ampliado por iniciativa propia** de la unica linea que el hallazgo cita a las otras tres visibles en el mismo `grep`, mas una quinta fuga fuera del patron del hallazgo (`| Rama | main |` en la cabecera de la plantilla del informe) — `D-040`. En este commit |
| F-009 — `D-021` afirma «cero menciones» y «17 menciones repartidas en 5 archivos» sin registrar el comando ni la descomposicion correcta (son 6 archivos) | Implementado | `T-035`, `Implementada` (2026-08-30), con **divergencia de forma declarada**. Se corrigio la enumeracion (cinco -> seis archivos) en el cuerpo de `D-021`, por ser un error factual. **No** se incrusto el comando dentro del cuerpo original: se anexo como nota fechada 2026-08-30, con la orden y su salida cruda, para no reescribir una entrada antigua como si el comando se hubiera anotado el 2026-08-28 — eso violaria `D-037`/`T-031`, decidida a peticion del propio auditor hace una sesion. Razonamiento completo en `D-038`. En este commit |
| F-010 — la excepcion de `C-002` (rutas absolutas duplicadas frente a `PROJECT.md`) solo vivia en `_audit/S-004.md`, ya entregado, y en ningun archivo operativo | Implementado | `T-036`, `Implementada` (2026-08-30). `C-002`, en `constraints.md`, conserva solo la restriccion normativa y remite a `PROJECT.md` para las rutas. En este commit |

Los cinco hallazgos de `R-004` quedan, con este commit, **implementados en el archivo operativo**.
Los cinco se verificaron de nuevo contra `HEAD` (no solo contra `31e2ff7`, que es el commit que
`R-004` audito) antes de aceptarlos: los cinco persistian. Comando y salida cruda de esa
verificacion, y de la de cada hallazgo por separado, viven en `D-038` y en `T-032` a `T-036`.

Ademas, **`T-037`** (`Origen: executor`, `Implementada`) no responde a un hallazgo numerado: nace
de la seccion 5.3 de `R-004`, donde el auditor contesto a nuestra pregunta «`D-021` no tiene
ninguna prueba automatizada que impida la regresion». Se escribio el control (`git grep` con el
patron `AIzar|Company_TripleS|github\.com`, acotado a `.claude/` y `CLAUDE.md`) como Paso 1b de
`protocol-close/SKILL.md`, con su primera ejecucion registrada en este mismo cierre.

`F-001` a `F-005` (de `R-002` y `R-003`) no entran en esta tabla: siguen cerrados por el auditor
sobre commits anteriores desde `S-009` y `S-011`.

## 1. Que se hizo

Llego `R-004`, la **tercera auditoria entregada del proyecto**, auditando `_audit/S-004.md` sobre
el commit `31e2ff7` (S-004, 2026-08-28). Entrego cinco hallazgos: `F-006`, `F-007`, `F-008` (todos
`Media`), `F-009` y `F-010` (`Baja`), los cinco `REVERSIBLE` a criterio (`D-020`: el inventario de
irreversibles sigue vacio hasta que `T-016` lo pueble, `T-016` no puede cerrarse sin alcance,
`T-004`).

- **Verificacion previa a aceptar**, contra `HEAD` (no solo contra `31e2ff7`): los cinco
  persistian. Los comandos ejecutados y su salida cruda viven en `D-038` (verificacion en bloque) y
  en cada tarea `T-032` a `T-036` (verificacion individual).
- **Acuse de recibo:** `_audit/index.md`, fila `S-004`, paso de `Pendiente` a `Con hallazgos`, con
  la ruta de `R-004.md` y los cinco codigos de hallazgo, y `Respondida en: S-012`.
- **`PROJECT.md`** gana la fila «Estado de los hallazgos» (ruta del `findings.md` del auditor,
  `T-032`) y una declaracion de **forma canonica POSIX** para sus rutas relativas —relativa, con
  `/`, desde la raiz de este repositorio— con las dos rutas absolutas como excepcion declarada
  (`T-033`, `D-039`).
- **`.claude/skills/protocol-start/SKILL.md`**, Paso 1c: el bloque `bash` vuelve a ser una orden
  ejecutable literal (`cat ../AIzar_Auditor/_review/index.md`), con la indireccion documentada al
  lado en vez de dentro del bloque, y un bloque «Tablero del auditor» **obligatorio y nunca
  omitido** en el reporte del Paso 3, para que saltarse el Paso 1c deje de ser indistinguible de
  haberlo corrido (`T-033`).
- **`.claude/skills/protocol-close/SKILL.md`**: cuatro codigos vivos (`T-014`, `D-018`, `T-015`)
  generalizados a `T-NNN`/`D-NNN` (`:349` y `:373-375`, `T-034`, con ampliacion propia de una linea
  a cuatro — `D-040`); la cabecera de la plantilla del informe corrige `| Rama | main |` por «la
  rama principal, segun `PROJECT.md`» (quinta fuga, fuera del patron de `F-008` y del de `F-009`,
  `D-040`); y una seccion nueva «1b. El control de fuga de datos del proyecto» al inicio del Paso 1,
  con el patron y el ambito acotado que exigia la pregunta de la seccion 5.3 de `R-004` (`T-037`).
- **`_persistence/constraints.md`**: `C-002` pierde las dos rutas absolutas literales y remite a
  `PROJECT.md`; conserva integra la frase normativa («`executor` no modifica archivos dentro del
  directorio del auditor») (`T-036`).
- **`_persistence/decisions.md`**: cuerpo de `D-021` corrige la enumeracion de archivos (cinco ->
  seis) y gana una nota anexa fechada 2026-08-30 con el comando y la salida cruda que sostienen
  «cero menciones» (`T-035`). Tres decisiones nuevas: **`D-038`** (evaluacion en bloque de los
  cinco hallazgos, las dos ampliaciones de alcance y la divergencia de forma sobre `F-009`),
  **`D-039`** (forma canonica POSIX, y por que el Paso 1c conserva la indireccion), **`D-040`**
  (las dos ampliaciones sobre `F-008` y la fuga adicional de `| Rama | main |`).
- **`_persistence/lessons.md`**: nueva entrada **`L-008`** — corregir un hallazgo puede romper la
  decision que lo genero; corre el control despues de arreglar, no solo antes. Documenta que al
  implementar `F-007` con la opcion (a) del hallazgo (ruta escrita en el bloque) el control de
  `D-021` dejo de dar cero, porque la correccion reintroducia el nombre del auditor dentro de
  `.claude/`. Se revirtio en menos de dos minutos y se tomo la opcion (b).
- **`_persistence/tasks.md`**: `T-032` a `T-037` nuevas, las seis `Implementada` (2026-08-30).
- **`_persistence/progress.md`**: nueva entrada `S-012` en la bitacora, con su fila de indice;
  reescritas las secciones 1 (Estado general), 2 (Ultimo realizado) y 3 (Siguiente paso).
- **`assumptions.md`**: sin cambios esta sesion.

**Coherencia indice/detalle** (Paso 2b del protocolo de cierre): se corrio sobre los siete archivos
de `_persistence/` antes del `git add`. Salio sin diferencias en seis; en `tasks.md` aparecio una
linea `C-002` que **no es una fila de indice huerfana**: es la salida cruda de un `sed -n` embebida
dentro de la entrada `T-036` (cita literal de `constraints.md` como evidencia de verificacion), que
el patron del control confunde con un encabezado `### C-002`. No se corrigio nada porque no hay
nada que corregir; se deja anotado por si el patron del control necesita afinarse.

**Control de fuga de datos** (Paso 1b, nuevo esta sesion por `T-037`):
`git grep -nE "AIzar|Company_TripleS|github\.com" -- .claude CLAUDE.md` sobre `HEAD` — sin
resultados (`exit 1`). Cero es la respuesta correcta.

## 2. Que NO se hizo, y por que

- **T-004** (alcance y objetivo del proyecto) sigue `No implementada` y bloqueando la etapa
  `000_preproject`. No se toco: no era el siguiente paso de esta sesion.
- **T-026** (disenar `_methodology/phases/001_descubrimiento.md`) sigue `No implementada` y
  `Bloqueante`. Depende de que `T-004` fije el alcance. No se toco.
- La sugerencia de `R-004` seccion 5.3 sobre `tasks.md:200` (nota historica del Paso 1c con la ruta
  antigua) **no se toco**: el propio informe la marca como historica y opcional («si se resuelve
  F-007, conviene que esa nota diga lo mismo»), no como hallazgo. Queda sin decidir si se alinea.
- No se identifico deuda tecnica nueva en esta sesion; `debt_tec.md` queda sin cambios.

## 3. Decisiones tomadas

- **`D-038`** — los cinco hallazgos de `R-004` se aceptan; `F-009` se corrige en el fondo
  (enumeracion de archivos) pero no en la forma que pide el hallazgo (incrustar el comando en el
  cuerpo de `D-021`), para no reescribir una entrada antigua como si hubiera llevado evidencia
  desde el origen. **Alternativas descartadas:** aplicar `F-009` al pie de la letra (mas comodo,
  descartada por chocar con `D-037`); aceptar `F-008` solo en la linea literal (descartada porque
  deja tres lineas identicas a la vista en el mismo `grep`); cerrar `F-010` aceptando el duplicado
  como `DT-XXX` (descartada porque el duplicado no aporta nada); rechazar `F-006` por desfase de
  commit (descartada, el hueco es el mismo).
- **`D-039`** — forma canonica POSIX de las rutas relativas de `PROJECT.md`, y el Paso 1c conserva
  la indireccion en vez de escribir la ruta dentro. **Alternativas descartadas:** escribir la ruta
  literal en el bloque (opcion (a) del hallazgo — se probo primero, revirtio `D-021`, se descarto);
  adoptar `\` como separador canonico (ataria los protocolos a un shell); un comando que extraiga
  el campo de `PROJECT.md` con `grep`/`sed` (fragil e ilegible frente al coste que ahorra).
- **`D-040`** — dos ampliaciones de alcance sobre `F-008`: cuatro lineas en vez de una, mas la
  quinta fuga (`| Rama | main |`) que ningun control existente detecta. **Alternativa descartada:**
  limitarse al alcance literal del hallazgo y abrir una tarea aparte para el resto — descartada
  porque es la misma edicion, en el mismo archivo.

Los tres se clasifican `REVERSIBLE` a criterio (`D-020`): ediciones de texto en archivos
versionados, sin borrado de datos, publicacion, migracion ni gasto, cada una deshacible con
`git revert`. Por eso decide `executor` y no se escala al usuario.

## 4. Supuestos vigentes y riesgos

Sin novedad respecto a `S-011`: `assumptions.md` mantiene `A-002` (un unico proyecto por
directorio), `A-003` (el proyecto arranca desde cero) y `A-004` (la comprobacion del desfase de la
guia tendra dueno cuando se copie), todos `Abierto`, ninguno tocado esta sesion. `A-001` sigue
`Confirmado`, sin cambios.

## 5. Siguiente tarea propuesta

**T-004** (`Alta`, `Bloqueante`) — recibir del usuario el alcance y objetivo del proyecto. Es el
bloqueo de fondo de la etapa `000_preproject`: bloquea a `T-025` y a `T-026` (disenar la fase
`Descubrimiento`, tambien `Bloqueante`), que no puede disenarse sin saber que construye el
proyecto.

## 6. Que pedimos auditar

- **Que la ampliacion de alcance sobre `F-008` no se haya quedado corta.** Se corrigieron cuatro
  lineas de codigos vivos mas la cabecera `| Rama | main |`, razonando que las demas apariciones
  del `grep` son citas de decisiones estructurales admitidas por el propio criterio de cierre del
  auditor. Pedimos que se contraste si esa frontera —«ejemplo ilustrativo» vs. «cita
  estructural»— esta bien trazada, o si dejamos algo del lado equivocado.
- **Que la divergencia de forma sobre `F-009` sea el balance correcto.** Se acepto el fondo del
  hallazgo (la enumeracion estaba mal) y se rechazo el remedio literal (incrustar el comando en el
  cuerpo original) citando `D-037`. Es una unica replica con evidencia nueva (`L-008` demuestra que
  el riesgo de `D-037` es real, no teorico), pero pedimos que se evalue si el criterio de
  «rige hacia adelante, no hacia atras» sigue siendo el correcto ahora que colisiona con una
  peticion explicita del propio auditor.
- **Que el control de `T-037` (Paso 1b) tenga el ambito correcto.** Se acoto a `.claude/` y
  `CLAUDE.md`, excluyendo `PROJECT.md` y el resto del arbol a proposito, siguiendo la advertencia
  de la propia seccion 5.3 de `R-004` sobre falsos positivos legitimos. Pedimos que se verifique
  que ese ambito no deja fuera un sitio donde una fuga real pasaria desapercibida — el propio caso
  de `| Rama | main |` (`D-040`) demuestra que hay al menos un patron de fuga que ningun control
  actual cubre.
