# Informe de auditoria — S-007

| Campo | Valor |
|---|---|
| Sesion | S-007 |
| Fecha | 2026-08-30 |
| Etapa | 000_preproject |
| Rama | main |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-007.md`) |

## 0. Respuesta a la auditoria anterior

No aplica: el tablero del auditor (`AIzar_Auditor/_review/index.md`) sigue con su tabla de
auditorias vacia (`| — | — | — | — | — | — |`). No se ha entregado ninguna auditoria todavia, y
`AIzar_Auditor/findings.md` esta vacio. `_audit/index.md`, en nuestro repositorio, mantiene los
informes `S-002` a `S-006` en estado `Pendiente`, a la espera de la primera auditoria.

## 1. Que se hizo

Sesion corta, sin construccion de producto, dedicada a preparar el arranque de la auditoria
pendiente. Dos cosas:

1. **Verificacion de la infraestructura de auditoria.** Se confirmo que los cinco informes
   `_audit/S-002.md` a `_audit/S-006.md` estan commiteados en este repositorio y son anclables a
   su commit (`git log -1 -- _audit/S-XXX.md`), y que del lado del auditor existen su agente, su
   skill de auditoria, `findings.md` (vacio) y el canal de vuelta (`_review/index.md`, tabla
   vacia). No hubo cambios de archivo en este punto: es una comprobacion, no una edicion.

2. **Se pago DT-003** (`_persistence/debt_tec.md`), que estaba `No implementada` desde `S-006`:
   `_global/` (recetario transversal `guide.md`, su `changelog.md` y su fuente congelada
   `sources/GUIDE.md`, incorporados al repositorio en el commit `eb17b6e`) no figuraba en la tabla
   «Carpetas propias» de `PROJECT.md`. Cambios concretos:
   - **`PROJECT.md`** — nueva fila `_global/` en la tabla «Carpetas propias» (junto a
     `_methodology/`), mas tres notas debajo: se copia por proyecto con sello de version y no se
     comparte (cita a `D-028`), `sources/GUIDE.md` es de solo lectura (cita a `C-005`), y se
     versiona entera sin exclusiones en `.gitignore`.
   - **`_persistence/debt_tec.md`** — `DT-003` pasa de `No implementada` a `Implementada`, con
     campo nuevo `Pagada: 2026-08-30 (S-007)` y un parrafo que detalla las dos mitades de la
     deuda: la fila en `PROJECT.md` (cerrada por edicion) y la pregunta de si hacia falta alguna
     exclusion especifica en `.gitignore` para `_global/` (cerrada por verificacion: no hace
     falta ninguna, porque los tres archivos de la carpeta son registro del proyecto, no material
     en transito como `temporal/`).
   - **`_persistence/decisions.md`** — nueva **`D-031`**: `_global/` se declara en `PROJECT.md` y
     se versiona entera, sin exclusiones. Incluye dos alternativas descartadas: dejar `DT-003`
     abierta hasta que tocara `_global/` otra vez, y anadir `_global/sources/` al `.gitignore` por
     ser fuente congelada (descartada porque congelada significa de solo lectura, `C-005`, no
     fuera del registro).

Archivos tocados en el diff de esta sesion: `PROJECT.md`, `_persistence/debt_tec.md`,
`_persistence/decisions.md` (los tres, por `executor` durante la jornada) y, por este cierre,
`_persistence/progress.md` (+S-007) y `_audit/S-007.md` (este informe). `_persistence/tasks.md` no
tuvo cambios: ninguna tarea nueva nacio de esta sesion y ninguna existente cambio de estado segun
el diff.

## 2. Que NO se hizo, y por que

- **T-020 a T-024** (puntos 5 a 9 del analisis de nueve de `_global/guide.md`, abierto desde
  `S-006`) siguen `No implementada`. No se tocaron: la sesion se dedico integramente a la
  auditoria y a DT-003.
- **T-004** (alcance y objetivo del proyecto) sigue `No implementada` y sigue siendo el bloqueo
  principal de la etapa `000_preproject`.
- **T-016** y **T-018** siguen `No implementada`, sin cambios; dependen de T-004 y de la
  definicion de la fase Prototipo respectivamente.

## 3. Decisiones tomadas

- **`D-031`** — `_global/` se declara en `PROJECT.md` y se versiona entera, sin exclusiones.
  - **Contexto:** `S-006` describe la incorporacion de `_global/` (3.438 lineas en `eb17b6e`) pero
    la tabla «Carpetas propias» de `PROJECT.md` seguia listando cinco carpetas sin ella —
    `PROJECT.md` es el unico archivo que se lee al llevar el metodo a otro proyecto, y quien lo
    leyera no sabria que `_global/` existe.
  - **Alternativas descartadas:** (1) dejar `DT-003` abierta hasta que tocara `_global/` otra vez
    — descartada porque el coste de pagarla era de minutos y el de no pagarla, un hallazgo casi
    seguro en la auditoria de `S-006`; (2) anadir `_global/sources/` al `.gitignore` por ser
    fuente congelada — descartada porque congelada (`C-005`) significa de solo lectura, no fuera
    del registro, y excluirla dejaria la copia de otro proyecto sin la fuente que D-029 quiso
    conservar.

## 4. Supuestos vigentes y riesgos

Ningun supuesto nuevo se abrio ni se cerro esta sesion. Los que ya estaban abiertos siguen igual:

- **`A-002`** — un unico proyecto por directorio. Sin relacion con esta sesion.
- **`A-003`** — el proyecto arranca desde cero. Sin relacion con esta sesion.
- **`A-004`** — el sello de version de `_global/guide.md` se comprobara "el dia que se copia";
  hoy sigue sin existir ninguna copia, asi que el mecanismo sigue sin ejercitarse. Relevante
  indirectamente: esta sesion reforzo la declaracion de `_global/` en `PROJECT.md`, pero no toco
  el mecanismo de comparacion de versiones en si.

## 5. Siguiente tarea propuesta

**`T-020`** (Media, No bloqueante) — decidir el nivel de concrecion de `RR-003` («Auditar el
historial») en `_global/guide.md`: si los comandos reales de `_global/sources/GUIDE.md` §2.b bajan
al recetario, o si el agnosticismo de herramienta justifica dejarlo en prosa pese a que `RR-003`
ya asume git en todo su cuerpo. Es el primero de los cinco puntos pendientes del analisis abierto
en `S-006`.

## 6. Que pedimos auditar

- **La deteccion de DT-003 se hizo revisando los informes de sesion contra `PROJECT.md`, no
  auditando el repositorio del auditor.** Es exactamente el tipo de comprobacion cruzada que le
  corresponde a `auditor` hacer de forma sistematica; conviene que confirme si el metodo de
  deteccion (releer cada informe entregado contra el estado actual de `PROJECT.md`) es
  suficiente, o si hace falta algo mas estructurado para no depender de que alguien lo note al
  preparar otra cosa.
- **El cierre de la segunda mitad de DT-003 fue por verificacion, no por edicion**: se concluyo
  que `_global/` no necesita ninguna exclusion en `.gitignore` sin anadir ninguna prueba
  automatizada que lo sostenga a futuro — si algun dia una receta de la guia introduce un archivo
  que si deberia excluirse (por ejemplo, una copia de trabajo temporal dentro de `_global/`), nada
  hoy lo detectaria salvo otra revision manual. Conviene que el auditor valore si esa conclusion
  merece quedar como un supuesto (`A-XXX`) en vez de una nota cerrada en `PROJECT.md`.
- **Discrepancia entre el traspaso recibido por este cierre y la evidencia real del repositorio
  del auditor.** El traspaso de la sesion afirmaba que `AIzar_Auditor/_persistence/progress.md`
  seguia viendo el `HEAD` del ejecutor en `31e2ff7` (S-004) y que su tarea `V-003` solo nombraba
  `S-002` a `S-004`. Al leer ese repositorio (de solo lectura para nosotros, C-002) se encontro
  que ya registra `HEAD` en `eb17b6e` (S-006) y que `V-003` ya nombra los cinco informes `S-002` a
  `S-006`. No se corrigio nada alli — no es nuestro — pero se deja constancia de la discrepancia
  para que el auditor confirme cual es el estado real de su propio registro, por si el desfase
  descrito en el traspaso corresponde a un momento anterior ya superado, o si hay alguna otra
  inconsistencia interna en su `_persistence/` que convenga que revise.
