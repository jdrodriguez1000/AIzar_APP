# Informe de auditoria — S-011

| Campo | Valor |
|---|---|
| Sesion | S-011 |
| Fecha | 2026-08-30 |
| Etapa | 000_preproject |
| Rama | main |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-011.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-003 — el eje reversible/irreversible se aplica en `CLAUDE.md` sin la salvedad que `D-020` exige (declarar la clasificacion cada vez que se usa) | Implementado | `T-029`, `Implementada` (2026-08-30). `CLAUDE.md`, seccion «Cuando no estemos de acuerdo», anade una vinieta que cita `D-020` y exige declarar la clasificacion en la propia respuesta mientras `T-016` no cierre. La misma exigencia se anadio a `.claude/skills/protocol-close/SKILL.md` (Paso 6b, seccion «Si el rechazo clasifica el asunto como reversible o irreversible, dilo como criterio»), porque `session-closer` no lee `decisions.md` entero. En este commit |
| F-004 — `D-020` remite a `T-015` donde la tarea que crea es `T-016` (`_persistence/decisions.md:504`) | Implementado | `T-030`, `Implementada` (2026-08-30). El cuerpo de `D-020`, en `_persistence/decisions.md`, corrige «Se crea T-015» a «Se crea T-016». Se toco una sola linea (`git diff --stat`: 1 insercion, 1 borrado); la segunda aparicion de la cadena `T-015` en ese archivo es la salida cruda del bloque de verificacion de `D-037` y se dejo intacta a proposito — es historia, no prosa a corregir (`L-007`). En este commit |
| F-005 — «Verificado antes de aceptar» (`D-018`) no registra que se ejecuto ni que devolvio | Implementado | `T-031`, `Implementada` (2026-08-30). `CLAUDE.md` (misma seccion) y el Paso 6 de `SKILL.md` («Una comprobacion concreta sobre `decisions.md`: las que verifican, con comando y salida») exigen desde ahora que toda decision con `Origen: auditor` que verifique algo lleve la orden ejecutada literal y su salida cruda, y no un veredicto («se comprobo», «verificado»). La regla rige hacia adelante; `D-018` no se reescribio, tal como ya se dejo dicho en `D-037` (S-010). En este commit |

Los tres hallazgos de `R-003` quedan, con este commit, **implementados en el archivo operativo**:
`git grep -l "D-020"` sobre `HEAD` deberia devolver ahora tambien `CLAUDE.md` y
`.claude/skills/protocol-close/SKILL.md`, ademas de `_audit/S-003.md`, `decisions.md`,
`progress.md` y `tasks.md`. No se corrio ese comando dentro de un bloque de verificacion de una
decision nueva porque **no se tomo ninguna decision nueva**: esta sesion ejecuto lo que `D-037`
(S-010) ya habia decidido, sin reabrir el criterio. Si el auditor quiere contrastarlo, el comando
de arriba y `git show <hash> -- CLAUDE.md .claude/skills/protocol-close/SKILL.md
_persistence/decisions.md` bastan.

`F-001` y `F-002` (de `R-002`) no entran en esta tabla: siguen cerrados por el auditor sobre
`beb782a` desde `S-009`.

## 1. Que se hizo

Se implementaron los tres hallazgos de `R-003`, evaluados y aceptados en `D-037` durante `S-010` y
dejados expresamente para esta jornada. No hubo evaluacion nueva ni decision nueva: fue ejecucion
mecanica de lo ya decidido.

- **`CLAUDE.md`**, seccion «Cuando no estemos de acuerdo»: nueva vinieta que cita `D-020` y exige
  declarar en la propia respuesta que la clasificacion reversible/irreversible se hizo a criterio,
  mientras `T-016` (poblar el inventario) no cierre. Aclara que los cuatro ejemplos entre
  parentesis (borrar datos, publicar, migrar, gastar) son ejemplos, no un inventario. Tambien
  seccion «Tratamiento de lo entregado por auditor»: nueva vinieta que exige comando y salida cruda
  en toda decision `Origen: auditor` que verifique algo, y que una entrada antigua que solo dijo
  «se comprobo» no se reescribe.
- **`.claude/skills/protocol-close/SKILL.md`**: dos secciones nuevas en el Paso 6/6b — «Una
  comprobacion concreta sobre `decisions.md`: las que verifican, con comando y salida» (detecta
  veredictos sin evidencia y ordena senalarlos en el reporte, no rellenarlos) y «Si el rechazo
  clasifica el asunto como reversible o irreversible, dilo como criterio» (misma exigencia que en
  `CLAUDE.md`, para que `session-closer` la vea sin tener que leer `decisions.md` entero). Tambien
  se actualizo la plantilla del reporte del Paso 8 para incluir la fila de decisiones que verifican
  sin evidencia.
- **`_persistence/decisions.md`**: el cuerpo de `D-020` corrige `T-015` -> `T-016`. Se verifico con
  `git diff --stat` que solo cambio una linea; la segunda aparicion de `T-015` en el archivo (la
  salida cruda de `git grep -n "Se crea T-015"` dentro del bloque de verificacion de `D-037`) se
  dejo tal cual.
- **`_persistence/tasks.md`**: `T-029`, `T-030` y `T-031` pasan de `No implementada` a
  `Implementada`, con `Fecha: 2026-08-30`, tanto en el indice como en su entrada de detalle.
- **`_persistence/lessons.md`**: nueva entrada **`L-007`** — «al corregir un codigo mal escrito, la
  salida cruda que lo cita no se toca» — documentando el hallazgo hecho al aplicar `T-030`: la
  cadena `T-015` aparecia dos veces y un reemplazo global habria reescrito la evidencia de `D-037`.
  Se relaciona explicitamente con `T-031`/`D-037` como la misma regla vista desde el otro lado.
- **`_persistence/progress.md`**: nueva entrada `S-011` en la bitacora, con su fila de indice;
  reescritas las secciones 1 (Estado general), 2 (Ultimo realizado) y 3 (Siguiente paso).

**Coherencia indice/detalle** (Paso 2b del protocolo de cierre): se corrio sobre los siete archivos
de `_persistence/` antes del `git add` y salio sin diferencias en los siete.

## 2. Que NO se hizo, y por que

- **T-004** (alcance y objetivo del proyecto) sigue `No implementada` y bloqueando la etapa
  `000_preproject`. No se toco en esta sesion: no era su siguiente paso.
- **T-026** (disenar `_methodology/phases/001_descubrimiento.md`) sigue `No implementada` y
  `Bloqueante`. Depende de que `T-004` fije el alcance. No se toco.
- No se registro ninguna decision nueva en `decisions.md`: todo lo hecho hoy ya estaba decidido en
  `D-037` (S-010). No hubo, por tanto, alternativas nuevas que descartar esta sesion.
- No se identifico deuda tecnica nueva en esta sesion.

## 3. Decisiones tomadas

**Ninguna decision nueva.** Esta sesion ejecuto `D-037` (tomada en `S-010`) sin reabrirla ni
modificar su criterio. No hay `D-XXX` que registrar.

## 4. Supuestos vigentes y riesgos

Sin novedad respecto a `S-010`: `assumptions.md` mantiene `A-002` (un unico proyecto por
directorio), `A-003` (el proyecto arranca desde cero) y `A-004` (la comprobacion del desfase de la
guia tendra dueno cuando se copie), todos `Abierto`, ninguno tocado esta sesion. `A-001` sigue
`Confirmado`, sin cambios.

## 5. Siguiente tarea propuesta

**T-004** (`Alta`, `Bloqueante`) — recibir del usuario el alcance y objetivo del proyecto. Es el
bloqueo de fondo de la etapa `000_preproject`: bloquea a `T-025` (juzgar las candidatas aplazadas
del recetario) y a `T-026` (disenar la fase `Descubrimiento`, tambien `Bloqueante`), que no puede
disenarse sin saber que construye el proyecto.

## 6. Que pedimos auditar

- **Que la implementacion de F-003/F-004/F-005 sea suficiente y este completa.** Esta sesion no
  volvio a evaluar los hallazgos, solo ejecuto lo ya decidido; pedimos que se contraste que el texto
  anadido a `CLAUDE.md` y a `SKILL.md` cumple exactamente lo que `D-037` prometio, y no una version
  debilitada o parcial de esa promesa.
- **Que `L-007` no sea una sobre-reaccion.** Se registro una leccion completa por un riesgo que, en
  este caso concreto, no llego a materializarse (se detecto antes del reemplazo, no despues).
  Pedimos opinion sobre si el umbral para escribir una leccion —«casi ocurre» frente a «ocurrio»—
  esta bien calibrado, o si esta inflando `lessons.md` con near-misses que no aportan tanto como una
  leccion nacida de un error real.
- **Que no anadir una `D-XXX` nueva para una sesion puramente ejecutiva sea el criterio correcto.**
  Esta sesion no registro ninguna decision propia, razonando que ejecutar lo ya decidido en `D-037`
  no es decidir. Si el auditor considera que la eleccion concreta de *donde* anadir cada vinieta
  (por ejemplo, en que seccion exacta de `CLAUDE.md`) si constituye una decision de forma que
  merecia su propio registro, es un punto debil real de este cierre.
