# Informe de auditoria — S-013

| Campo | Valor |
|---|---|
| Sesion | S-013 |
| Fecha | 2026-08-30 |
| Etapa | 000_preproject |
| Rama | main |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-013.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-011 — la tabla de Convenciones de `Origen` en `tasks.md` enumeraba tres valores (`usuario`/`executor`/`auditor`) cuando ya se usaban cinco (`VS-XXX`, `metodo` incluidos) | Implementado | `T-038`, `Implementada` (2026-08-30). La tabla admite los cinco valores reales, con tabla explicativa de que significa cada uno y el criterio para admitir uno nuevo («nombra un origen de demanda que ninguno de los cinco ya cubre», entra en la tabla en la misma pasada que la primera tarea que lo use). En este commit |
| F-012 — `D-024` reparte el veredicto de un Gate en tres actos y da por hecho que el acto 2 (dictamen del auditor) tendra donde escribirse, cuando `contract.md` §4 solo define una forma de entrega (`R-XXX` audita `S-XXX`, 1:1) que no cubre un dictamen de Gate | Aceptado — pendiente | `A-005` (`assumptions.md`, `Abierto`) mas `T-039` (`tasks.md`, `No implementada`), disparador «al diseñar la fase Prototipo» (`D-042`). Asunto **bilateral**: la solucion asigna al auditor una funcion que su propio contrato no le da hoy, y `contract.md` no se cambia por un lado. Se eleva al usuario y a la sesion principal del auditor |
| F-013 — la version de `contract.md` no se comparaba nunca contra la registrada en `PROJECT.md`: el mecanismo de deteccion de divergencia existia en el papel y no se ejecutaba en ningun protocolo | Implementado | `T-040`, `Implementada` (2026-08-30), importancia subida de `Media` (la del hallazgo) a `Alta` en nuestro registro. Nuevo **Paso 1d** en `.claude/skills/protocol-start/SKILL.md`, con la misma indireccion del Paso 1c. Primera ejecucion registrada con comando y salida cruda: version 1 coincide, sin desfase. En este commit |
| F-014 — `Origen` en `debt_tec.md` cargaba texto de confirmacion no admitido por su convencion en `DT-001` y `DT-002` («PROPUESTA — pendiente de confirmar») | Implementado | `T-041`, `Implementada` (2026-08-30). Campo nuevo **`Confirmacion`** (`Confirmada` / `Propuesta (pendiente de <quien>)`), con columna en el indice; `Origen` vuelve a sus tres valores. Ampliado por iniciativa propia: se encontro un tercer caso que el hallazgo no señalaba, `DT-003`, haciendo lo mismo por la otra punta («confirmada por el usuario el 2026-08-30» dentro de `Origen`). Las tres entradas quedan `Confirmada`. En este commit |
| F-015 — el numero de decisiones de `S-005` no coincidia entre `_audit/S-005.md`, el mensaje del commit `e9216f6` y `_persistence/progress.md` | Implementado | Verificado contra `HEAD`: la cifra que se podia corregir **ya no existe** en el registro vivo (la linea citada por el hallazgo era la fila «Avance de la etapa», que se reescribe cada sesion, y se sobrescribio sola); las otras dos cifras viven en un informe ya entregado y en un mensaje de commit, inmutables por `D-018`. **No se reescribio nada.** Se acepta el hallazgo como **regla hacia adelante**: en los informes siguientes el numero de decisiones sale de la propia enumeracion y no se escribe aparte (`D-041`). En este commit, como regla aplicada de aqui en adelante — no como correccion retroactiva |

Los cinco hallazgos de `R-005` se aceptan, cero rechazos (`D-041`). Los cinco se verificaron de
nuevo contra `HEAD` (`57f3f2a`, no contra `e9216f6`, que es el commit que `R-005` audito) antes de
aceptarlos: cuatro persistian, uno (`F-015`) no. Comando y salida cruda de esa verificacion viven
en cada `T-XXX` (`T-038`, `T-039`, `T-040`, `T-041`) y en `D-041` para `F-015`.

Ademas, dos tareas sin numero de hallazgo:

- **`T-042`** (`Origen: executor`, `Implementada`) — no responde a un hallazgo: nace de correr el
  control de `L-008` (correr el control de regresion despues de tocar `.claude/`, no solo antes)
  tras la edicion de `T-040`. Encontro un ultimo codigo vivo (`DT-003`) que `T-034` (sesion `S-012`)
  no habia generalizado, y que ademas ya era **falso**: `DT-003` esta `Implementada`, no
  `Cancelada`. Corregido a `DT-NNN`.
- **`T-043`** (`Origen: auditor`, `No implementada`) — no es un hallazgo numerado: es la respuesta
  de `R-005` §5.1 a nuestra pregunta sobre si el parche de meter `_methodology/phases/` (contenido
  propio) dentro de una carpeta declarada agnostica era aceptable. El auditor confirma que si, pero
  recomienda que la exclusion se marque **dentro** de `_methodology/`, no solo en `PROJECT.md`.
  Aceptada en el fondo, **aplazada** porque decidir donde escribir la marca presupone la regla
  agnostico/propio que `DT-002` declara sin decidir (`D-044`); disparador: al cerrar `T-004` y
  antes de escribir la primera fase.

Un segundo asunto bilateral, fuera de la tabla de hallazgos porque nace de una pregunta nuestra a
`R-005` §5.2, no de un hallazgo del auditor: se pregunto si convenia que `contract.md` **citara**
`PROJECT.md` en vez de repetir sus valores, para pagar `DT-001`. El auditor **rechazo** la
propuesta — un contrato bilateral no puede colgar de un archivo que solo una de las dos partes
controla y puede cambiar sin avisar. El rechazo se **acepta** (`D-043`, sin replica: el auditor no
contradice ningun hecho nuestro, da un motivo estructural mejor que el nuestro). `DT-001` queda con
una sola via de pago: detectar la divergencia, que es exactamente `F-013`/`T-040`.

`F-001` a `F-010` (de `R-002`, `R-003` y `R-004`) no entran en esta tabla: siguen cerrados por el
auditor sobre commits anteriores desde `S-009`, `S-010`/`S-011` y `S-012`.

## 1. Que se hizo

Llego `R-005`, la **cuarta auditoria entregada del proyecto**, auditando `_audit/S-005.md` sobre el
commit `e9216f6` (S-005, 2026-08-28). Entrego cinco hallazgos: `F-011`, `F-012`, `F-013` (todos
`Media`), `F-014` y `F-015` (`Baja`), los cinco `REVERSIBLE` a criterio (`D-020`: el inventario de
irreversibles sigue vacio hasta que `T-016` lo pueble, `T-016` no puede cerrarse sin alcance,
`T-004`). El auditor declara haber contrastado 28 afirmaciones del informe de sesion, de las que 23
se sostienen; ninguno de los cinco hallazgos toca la sustancia de la sesion —el canonico entro
entero, el renombrado `FT-`/`SC-` no dejo residuos, `_product/` no existe porque asi se declaro—;
los cinco tocan el registro que debe sobrevivir a la sesion.

- **Verificacion previa a aceptar**, contra `HEAD` (no solo contra `e9216f6`): cuatro persistian,
  uno no. Los comandos ejecutados y su salida cruda viven en cada tarea (`T-038` a `T-041`) y en
  `D-041` (verificacion en bloque, incluida la de `F-015`).
- **Acuse de recibo:** `_audit/index.md`, fila `S-005`, paso de `Pendiente` a `Con hallazgos`, con
  la ruta de `R-005.md` y los cinco codigos de hallazgo. `Respondida en: S-013` se rellena en este
  mismo commit.
- **`_persistence/tasks.md`**: tabla de Convenciones de `Origen` con los cinco valores reales,
  tabla explicativa y criterio de admision de un valor nuevo (`T-038`). Seis tareas nuevas:
  `T-038`, `T-040`, `T-041`, `T-042` (`Implementada`), `T-039`, `T-043` (`No implementada`, cada
  una con su disparador escrito).
- **`.claude/skills/protocol-start/SKILL.md`**: Paso 1d nuevo, que compara la version declarada de
  `contract.md` contra la registrada en `PROJECT.md`, con las dos salidas obligatorias en el
  reporte (coincide / desfase) — omitir el caso «coincide» dejaria el silencio ambiguo, el mismo
  fallo que `F-007` en `R-004` (`T-040`). Ademas, generalizacion de un ultimo codigo vivo
  (`DT-003` → `DT-NNN`) que `T-034` habia dejado sin tocar (`T-042`).
- **`_persistence/debt_tec.md`**: campo nuevo `Confirmacion` en el indice y en la plantilla,
  separado de `Estado` (`Estado` = si se pago; `Confirmacion` = si es deuda) (`T-041`, `D-045`).
  `DT-001` reescribe su via de pago: la propuesta de que `contract.md` cite `PROJECT.md` queda
  cerrada por rechazo del auditor, y se documenta la via descartada en vez de sustituirla en
  silencio (`D-043`). `DT-002` gana disparador («al cerrar `T-004`, antes de escribir la primera
  fase»), el mismo que `T-043` (`D-044`).
- **`_persistence/assumptions.md`**: nueva **`A-005`** — el dictamen de un Gate cabe en la forma de
  entrega que `contract.md` §4 ya define. Supuesto sobre el que `D-024` ya esta construida, `Abierto`,
  disparador «al diseñar la fase Prototipo», tarea relacionada `T-039`.
- **`_persistence/decisions.md`**: cinco decisiones nuevas. **`D-041`** (los cinco hallazgos de
  `R-005` se aceptan; razonamiento completo de por que `F-015` no se reescribe). **`D-042`** (el
  dictamen de Gate se registra por partida doble —supuesto y tarea— y la mitad bilateral se eleva).
  **`D-043`** (se acepta el rechazo del auditor sobre `contract.md` citando a `PROJECT.md`; queda
  el detector como unica via de pago de `DT-001`). **`D-044`** (la marca de no copiar `phases/` se
  aplaza porque presupone la regla que `DT-002` no ha decidido). **`D-045`** (la deuda gana un eje
  propio, `Confirmacion`, separado de `Estado`).
- **`_persistence/lessons.md`**: nueva **`L-009`** — un `grep` estructural sobre el registro vivo
  cuenta como entrada real una linea que en verdad es salida cruda citada dentro de un bloque de
  codigo cercado. Nace de un reporte del `session-starter` al arranque de esta jornada, que senalo
  una entrada `### C-002` huerfana en `_persistence/tasks.md:983`; comprobado con `awk`/`sed` que
  esa linea vive dentro de un bloque cercado (fences en 981 y 989), parte de la salida cruda de
  `T-036` (sesion anterior). No se borro nada.
- **`_persistence/progress.md`**: nueva entrada `S-013` en la bitacora, con su fila de indice;
  reescritas las secciones 1 (Estado general), 2 (Ultimo realizado) y 3 (Siguiente paso).
- **`_persistence/constraints.md`**: sin cambios esta sesion; ningun hallazgo de `R-005` introdujo
  una restriccion nueva.

**Coherencia indice/detalle** (Paso 2b del protocolo de cierre): se corrio sobre los siete archivos
de `_persistence/` antes del `git add`. Salio sin diferencias en seis; en `tasks.md` aparecio de
nuevo la misma linea `C-002` que ya se anoto en `_audit/S-012.md` — no es una fila de indice
huerfana, es la salida cruda de `T-036` (verificacion de `F-010`, sesion anterior), dentro de un
bloque cercado. Es exactamente el caso que motiva `L-009` de esta misma sesion: se comprobo el
contexto (fences en 981 y 989 de `tasks.md`) antes de tratarla como desfase, y no lo era.

**Control de fuga de datos** (Paso 1b): `git grep -nE "AIzar|Company_TripleS|github\.com" --
.claude CLAUDE.md` sobre `HEAD` — sin resultados (`exit 1`). Cero es la respuesta correcta.

## 2. Que NO se hizo, y por que

- **T-004** (alcance y objetivo del proyecto) sigue `No implementada` y bloqueando la etapa
  `000_preproject`. No se toco: no era el siguiente paso de esta sesion.
- **T-026** (disenar `_methodology/phases/001_descubrimiento.md`) sigue `No implementada` y
  `Bloqueante`. Depende de que `T-004` fije el alcance. No se toco.
- **T-039** (acordar con el auditor la forma de entrega del dictamen de Gate) queda `No
  implementada` a proposito: su disparador es «al diseñar la fase Prototipo», que no ha llegado.
  Adelantarla seria la especulacion que `D-027` prohibe.
- **T-043** (marcar dentro de `_methodology/` que `phases/` no se copia) queda `No implementada` a
  proposito: su disparador es «al cerrar T-004, antes de escribir la primera fase», que tampoco ha
  llegado. Escribirla hoy fijaria por la puerta de atras la regla agnostico/propio que `DT-002`
  declara sin decidir.
- No se identifico deuda tecnica nueva propuesta por este cierre; los cambios en `debt_tec.md` esta
  sesion son correcciones de estructura (`Confirmacion`) y de contenido (`DT-001`, `DT-002`)
  derivadas de hallazgos ya aceptados, no deuda nueva.

## 3. Decisiones tomadas

- **`D-041`** — los cinco hallazgos de `R-005` se aceptan; `F-015` se acepta como regla hacia
  adelante y no se reescribe nada retroactivo. **Alternativa descartada:** reescribir la cifra en
  `_audit/S-005.md` o en el mensaje del commit `e9216f6` — descartada porque convertiria «hubo un
  error de conteo» en «no hubo ninguno», la misma falsificacion que `D-037`/`L-007` prohiben.
- **`D-042`** — el dictamen de Gate se registra por partida doble (`A-005` + `T-039`), y la mitad
  bilateral se eleva al usuario. **Alternativa descartada:** elegir solo una de las dos formas que
  ofrecia el auditor (supuesto **o** tarea) — descartada porque responden preguntas distintas: si
  seguimos construyendo sobre algo no confirmado, y quien tiene que resolverlo y cuando.
- **`D-043`** — se acepta el rechazo del auditor sobre `contract.md` citando a `PROJECT.md`.
  **Alternativa descartada:** replicar el rechazo — descartada porque no hay evidencia nueva que
  aportar, solo el mismo argumento con otras palabras, que `CLAUDE.md` no admite como replica.
- **`D-044`** — la marca de no copiar `phases/` se aplaza. **Alternativa descartada:** escribir ya
  un `README.md` provisional en `_methodology/` — descartada porque un archivo «provisional» dentro
  de la carpeta que se copia a otros proyectos viaja con ella, y deja de ser provisional en cuanto
  alguien la copia.
- **`D-045`** — campo `Confirmacion` propio, separado de `Estado`. **Alternativa descartada:** dejar
  el matiz en prosa dentro del cuerpo de cada entrada — descartada porque el ojo entra por la tabla
  del indice, y lo que solo esta en el detalle no se relee (mismo argumento que `F-011` en otro
  archivo el mismo dia).

Las cinco se clasifican `REVERSIBLE` a criterio (`D-020`): ediciones de texto en archivos
versionados, sin borrado de datos, publicacion, migracion ni gasto, cada una deshacible con
`git revert`. Coincide con la clasificacion que el propio auditor declara en `R-005`. Por eso
decide `executor` y no se escala al usuario — salvo en la parte bilateral de `D-042` y `D-043`, que
no es una decision de contenido sino de a quien le toca decidir, y esa si se eleva.

## 4. Supuestos vigentes y riesgos

- **`A-005`** (nueva, `Abierto`) — el dictamen de un Gate cabe en la forma de entrega que
  `contract.md` §4 ya define. Se apoya en `D-024` (el reparto de los tres actos del Gate, escrito
  en `PROJECT.md` y `CLAUDE.md`). Si resulta falso, **el Gate 1 no se puede emitir tal como esta
  escrito** el dia que llegue: `executor` esperaria un dictamen que ninguna skill del auditor sabe
  producir. Se valida cuando `contract.md`, en version posterior a la 1, declare una segunda forma
  de entrega para el dictamen o diga explicitamente que usa la misma. Disparador de la accion:
  al diseñar la fase Prototipo.
- Sin cambios en `A-002` (un unico proyecto por directorio), `A-003` (el proyecto arranca desde
  cero) ni `A-004` (la comprobacion del desfase de la guia tendra dueno cuando se copie), los tres
  `Abierto`, ninguno tocado esta sesion. `A-001` sigue `Confirmado`.

## 5. Siguiente tarea propuesta

**T-004** (`Alta`, `Bloqueante`) — recibir del usuario el alcance y objetivo del proyecto. Sigue
siendo el bloqueo de fondo de la etapa `000_preproject`: bloquea a `T-025`, a `T-026` (diseno de la
fase `Descubrimiento`) y, desde esta sesion, tambien al disparador de `T-043`.

## 6. Que pedimos auditar

- **Que la clasificacion de `F-015` como «regla, no correccion» no sea una forma comoda de cerrar
  un hallazgo sin tocar nada.** Verificamos que la cifra corregible ya no existia en el registro
  vivo y que las otras dos son inmutables por `D-018`; pedimos que se contraste si esa lectura es
  correcta o si existia una tercera via de correccion que no consideramos.
- **Que el registro doble de `F-012` (`A-005` + `T-039`) no sea sobre-ingenieria frente a la
  sugerencia original del auditor de elegir una sola forma.** Argumentamos que responden preguntas
  distintas; pedimos que se evalue si esa distincion se sostiene o si un solo registro habria
  bastado.
- **Que la importancia subida de `T-040` (de `Media` a `Alta`) este bien fundamentada.** La subimos
  porque protege el unico documento que obliga a las dos terminales y porque el hallazgo no tenia
  ninguna defensa previa (0 ejecuciones del Paso nuevo antes de esta sesion). Pedimos que se
  contraste si `Alta` es proporcionado o es una sobrevaloracion de nuestro propio trabajo.
- **Que la parte bilateral de esta sesion (`D-042`, `D-043`, `A-005`) quede correctamente elevada.**
  No decidimos ninguna de las dos por nuestra cuenta, pero somos la parte que redacta el registro de
  las dos: pedimos que se verifique que la forma en que las describimos no prejuzga el resultado que
  nos convendria.
